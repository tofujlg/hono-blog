{
  "name": "hono-blog",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.tsx",
    "build": "bun run src/build.ts",
    "migrate": "bun run scripts/migrate.ts"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "marked": "^15.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
